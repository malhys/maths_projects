\documentclass[12pt,a4paper]{article}

%\usepackage[left=1.5cm,right=1.5cm,top=1cm,bottom=2cm]{geometry}
\usepackage[in, plain]{fullpage}
\usepackage{array}
\usepackage{../../../pas-math}
\usepackage{../../../moncours}

\usepackage{multicol}
\usepackage{caption}
\usepackage{tikz}
\usetikzlibrary{trees}


%\usepackage{pas-cours}
\input{../../../utils_maths}

\date{}
\title{}

\input{./derivee_bases}
\newcommand{\disp}{true}

\begin{document}
	%\maketitle
	\chap[num=4, color=red]{Dérivée et sens de variation d'une fonction}{}
	
	\section{Tangente et nombre dérivé}
	
	
	\begin{myprops}
	\begin{multicols}{2}
		

	
	
	
	On considère la courbe représentative de la fonction $f$, définie par $f(x) = x^2 + 2x - 1$ ; cette courbe est une parabole.\\
	
	
	La droite d'équation $y = 2x - 1$ possède \kw{un seul point commun avec la parabole}, le point $(0\, ; 1)$. La droite est la \kw{tangente} en 0 de la parabole.
	La \kw{pente de la tangente} est $2$, c'est le \kw{nombre dérivé} en 0. 
	
	\includegraphics[scale=0.5]{./img/ex}
	\end{multicols}
	\end{myprops}
	
	\section{Fonction dérivée}
	
	\begin{mydef}
		Soit une fonction $f$. Pour tout $x$, la \kw{fonction dérivée} $f'$ est la fonction qui donne le nombre dérivé en $x$ (noté \kw{$f'(x)$}). 
		
		Une fonction est \kw{dérivable} en $x$ si et seulement si sa fonction dérivée $f'$ est définie en $x$.
	\end{mydef}
	
	\begin{myex}
		On considère la fonction $f$ définie par 
		\begin{align*}
		f(x) = x^2+2x-1
		\end{align*}
		
		Sa fonction dérivée, est la fonction $f'$ telle que:
		\begin{align*}
			f'(x) = 2x+2
		\end{align*}
	\end{myex}
	
	\newpage 
	
\section{Dérivation d'une fonction}
	
	\subsection{Dérivées des fonctions usuelles}
	
	Pour dériver une fonction, on utilise les formule du tableau ci dessous :
	
	\begin{center}
		\drv{\disp}
	\end{center}

	\subsection{Opérations sur les fonctions dérivables}
	
	$u$ et $v$ sont deux fonctions dérivables.
	
	\begin{center}
		\drvOp{\disp}
	\end{center}

\section{Utilisation de la dérivée}
	
	\begin{myact}{1}
		\input{activite2}
	\end{myact}
	
	\begin{myprop}
		Les variations d'une fonction en un point sont liées au signe du nombre dérivé de la fonction en ce point :
		\begin{itemize}
			\item Si \kw{$f'(x) > 0$}, alors la fonction \kw{$f$ est croissante} en $x$.
			\item Si \kw{$f'(x)=0$}, alors la fonction \kw{$f$ est constante} en $x$.
			\item Si \kw{$f'(x) < 0$}, alors la fonction \kw{$f$ est décroissante} en $x$. 
		\end{itemize}
	\end{myprop}
	
	\begin{myex}
		On considère la fonction $f$, définie sur $[-2;2]$ par $f(x)=-3x^2+6x-4$.
		
		Sa fonction dérivée $f'$ est définie par $f'(x)= -6x +6$.
		
		On résous l'inéquation $f'(x) > 0$.
		
		\begin{align*}
			-6x +6 &> 0 \\
			-6x &> -6 \\
			x &< \frac{-6}{-6} \\
			x &< 1
		\end{align*}
		
		Donc la fonction $f$ est croissante avant $1$ et décroissante après.
		
		On a donc :
		
		\begin{center}
			\begin{tikzpicture}[scale=0.8]
				\tkzTabInit{$x$/1.5,$f'(x)$/1.5,$f(x)$/4}{$-2$, $1$, $2$}
				\tkzTabLine { ,+ ,z , -, }
				\tkzTabVar{-/$-28$,+/$-1$,-/$-4$}
			\end{tikzpicture}


%			\begin{tikzpicture}[scale=0.8]
%				\tkzTabInit{$x$/1.5,$f'(x)$/1.5,$f(x)$/4}{$-12$, $-5$, $7$, $20$}
%				\tkzTabLine { ,- ,z , +, z, -}
%				\tkzTabVar{+/$7$,-/$-4$,+/$-1$,-/$-6$}
%			\end{tikzpicture}
		\end{center}
		
	\end{myex}
	
\end{document}